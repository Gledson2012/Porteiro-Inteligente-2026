# Configuração de assinatura do APK
# Copie este arquivo como keystore.properties e preencha com seus dados
# NÃO commite o keystore.properties real (está no .gitignore)

storePassword=sua_senha_store
keyPassword=sua_senha_chave
keyAlias=seu_alias
storeFile=release.keystore
